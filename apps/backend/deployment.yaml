apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
        version: 00918fc-main
    spec:
      containers:
      - name: backend
        image: robrich/backend:00918fc-main
        ports:
        - containerPort: 5000
        envFrom:
        - configMapRef:
            name: backend
        resources: {}
