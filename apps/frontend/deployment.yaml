apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
spec:
  replicas: 2
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
        version: 8b40a8b-main
    spec:
      containers:
      - name: frontend
        image: robrich/frontend:8b40a8b-main
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: frontend
        resources: {}
